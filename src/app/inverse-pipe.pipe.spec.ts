import { InversePipePipe } from './inverse-pipe.pipe';

describe('InversePipePipe', () => {
  it('create an instance', () => {
    const pipe = new InversePipePipe();
    expect(pipe).toBeTruthy();
  });
});
